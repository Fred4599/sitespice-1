---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="About SiteSpice - Web Development & SEO by Braydon Carter">
  <!-- Hero Video Background -->
  <div class="fixed inset-0 -z-30 w-full h-full overflow-hidden pointer-events-none">
    <video 
      id="about-hero-video"
      autoplay 
      loop 
      muted 
      playsinline 
      preload="none"
      class="w-full h-full object-cover opacity-20 mix-blend-screen" 
      style="filter: hue-rotate(150deg) contrast(1.2);">
      <source src="https://cdn.coverr.co/videos/coverr-digital-lines-moving-background-4770/1080p.mp4" type="video/mp4">
    </video>
    <div class="absolute inset-0 bg-gradient-to-b from-black via-black/80 to-black"></div>
  </div>

  <!-- Background Gradient -->
  <div class="fixed inset-0 -z-20 bg-gradient-to-b from-purple-900/5 via-black to-black pointer-events-none"></div>

  <!-- Technical Grid Overlay -->
  <div class="fixed inset-0 pointer-events-none z-0 technical-grid opacity-30"></div>

  <!-- Vertical Structure Lines -->
  <div class="fixed inset-0 pointer-events-none z-0 max-w-[1400px] mx-auto border-x border-white/[0.04]">
    <div class="absolute left-1/4 h-full w-px bg-white/[0.02]"></div>
    <div class="absolute left-2/4 h-full w-px bg-white/[0.02]"></div>
    <div class="absolute left-3/4 h-full w-px bg-white/[0.02]"></div>
  </div>

  <Navigation />

  <main class="z-10 relative">
    <!-- Hero Section -->
    <section class="pt-32 pb-20 px-6">
      <div class="max-w-[1400px] mx-auto">
        <div class="text-center mb-16 animate-on-scroll" style="animation: animationIn 0.8s ease-out forwards;">
          <p class="text-purple-400 text-sm font-medium tracking-wider mb-4 uppercase">About SiteSpice</p>
          <h1 class="text-5xl md:text-7xl font-bold mb-6 bg-gradient-to-r from-white via-purple-200 to-white bg-clip-text text-transparent">
            Web Development & SEO<br/>by Braydon Carter
          </h1>
        </div>
      </div>
    </section>

    <!-- Origin Story Section -->
    <section class="py-20 px-6">
      <div class="max-w-[900px] mx-auto">
        <div class="backdrop-blur-xl bg-white/[0.02] border border-white/[0.08] rounded-3xl p-12 md:p-16 animate-on-scroll" style="animation: animationIn 0.8s ease-out 0.2s forwards; animation-play-state: paused;">
          <div class="flex items-center gap-3 mb-8">
            <div class="w-12 h-12 rounded-full bg-gradient-to-br from-purple-500 to-purple-700 flex items-center justify-center">
              <iconify-icon icon="mdi:origin" class="text-2xl text-white"></iconify-icon>
            </div>
            <h2 class="text-3xl md:text-4xl font-bold">SiteSpice Origin</h2>
          </div>
          
          <h3 class="text-2xl font-semibold mb-6 text-purple-300">Our Story</h3>
          
          <div class="space-y-6 text-neutral-300 leading-relaxed text-lg">
            <p>
              Founded by Braydon Carter, SiteSpice began with a simple mission: to help business professionals who often say, <span class="text-white font-medium">"I'm just not techy."</span>
            </p>
            <p>
              What started as a focus on aesthetics—creating beautiful, eye-catching sites—quickly evolved when Braydon realized that good looks alone don't generate results. He began integrating software tools like forms, calendars, surveys, and automations to make sites work harder for business owners.
            </p>
            <p>
              When clients faced the traffic problem—even well-built sites with great tools weren't getting seen—Braydon dove head-first into SEO, building a system that drives real results at a single flat rate because he believes in going all-in with clients.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Values Section -->
    <section class="py-20 px-6">
      <div class="max-w-[1400px] mx-auto">
        <div class="text-center mb-16 animate-on-scroll" style="animation: animationIn 0.8s ease-out 0.3s forwards; animation-play-state: paused;">
          <h2 class="text-4xl md:text-5xl font-bold mb-4">Our Values</h2>
          <p class="text-neutral-400 text-lg">The core principles that guide our work and relationships.</p>
        </div>

        <div class="grid md:grid-cols-3 gap-8 spotlight-grid">
          <!-- Real Results -->
          <div class="backdrop-blur-xl bg-white/[0.02] border border-white/[0.08] rounded-2xl p-8 spotlight-card relative animate-on-scroll" style="animation: animationIn 0.8s ease-out 0.4s forwards; animation-play-state: paused;">
            <div class="relative z-10">
              <div class="w-14 h-14 rounded-2xl bg-gradient-to-br from-purple-500 to-purple-700 flex items-center justify-center mb-6">
                <iconify-icon icon="mdi:chart-line" class="text-3xl text-white"></iconify-icon>
              </div>
              <h3 class="text-2xl font-bold mb-4">Real Results</h3>
              <p class="text-neutral-300 leading-relaxed">
                We focus on delivering websites that not only look great but also drive measurable business results through strategic functionality.
              </p>
            </div>
          </div>

          <!-- Personal Relationships -->
          <div class="backdrop-blur-xl bg-white/[0.02] border border-white/[0.08] rounded-2xl p-8 spotlight-card relative animate-on-scroll" style="animation: animationIn 0.8s ease-out 0.5s forwards; animation-play-state: paused;">
            <div class="relative z-10">
              <div class="w-14 h-14 rounded-2xl bg-gradient-to-br from-purple-500 to-purple-700 flex items-center justify-center mb-6">
                <iconify-icon icon="mdi:handshake" class="text-3xl text-white"></iconify-icon>
              </div>
              <h3 class="text-2xl font-bold mb-4">Personal Relationships</h3>
              <p class="text-neutral-300 leading-relaxed">
                We believe in real, personal relationships. You won't get handed off to an overseas team. Braydon meets and takes care of every client himself.
              </p>
            </div>
          </div>

          <!-- Going All-In -->
          <div class="backdrop-blur-xl bg-white/[0.02] border border-white/[0.08] rounded-2xl p-8 spotlight-card relative animate-on-scroll" style="animation: animationIn 0.8s ease-out 0.6s forwards; animation-play-state: paused;">
            <div class="relative z-10">
              <div class="w-14 h-14 rounded-2xl bg-gradient-to-br from-purple-500 to-purple-700 flex items-center justify-center mb-6">
                <iconify-icon icon="mdi:poker-chip" class="text-3xl text-white"></iconify-icon>
              </div>
              <h3 class="text-2xl font-bold mb-4">Going All-In</h3>
              <p class="text-neutral-300 leading-relaxed">
                We believe in going all-in with our clients. That's why we offer single, comprehensive packages rather than tiered services that limit what we can achieve together.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Meet Braydon Section -->
    <section class="py-20 px-6">
      <div class="max-w-[1200px] mx-auto">
        <div class="backdrop-blur-xl bg-white/[0.02] border border-white/[0.08] rounded-3xl overflow-hidden animate-on-scroll" style="animation: animationIn 0.8s ease-out 0.7s forwards; animation-play-state: paused;">
          <div class="grid md:grid-cols-2 gap-0">
            <!-- Image Side -->
            <div class="relative h-[400px] md:h-auto">
              <img 
                src="/assets/braydon-carter.jpg" 
                alt="Braydon Carter - Founder of SiteSpice" 
                class="w-full h-full object-cover"
                onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
              />
              <div class="hidden absolute inset-0 bg-gradient-to-br from-purple-900/30 to-purple-700/30 items-center justify-center">
                <div class="w-32 h-32 rounded-full bg-gradient-to-br from-purple-500 to-purple-700 flex items-center justify-center">
                  <iconify-icon icon="mdi:account" class="text-6xl text-white"></iconify-icon>
                </div>
              </div>
            </div>

            <!-- Content Side -->
            <div class="p-12 md:p-16">
              <div class="flex items-center gap-3 mb-8">
                <div class="w-12 h-12 rounded-full bg-gradient-to-br from-purple-500 to-purple-700 flex items-center justify-center">
                  <iconify-icon icon="mdi:account-star" class="text-2xl text-white"></iconify-icon>
                </div>
                <h2 class="text-3xl md:text-4xl font-bold">Meet Braydon</h2>
              </div>
              
              <p class="text-neutral-400 mb-6 text-sm uppercase tracking-wider">The founder behind SiteSpice's success.</p>
              
              <div class="space-y-6 text-neutral-300 leading-relaxed">
                <p>
                  I have worked as a speaker, coach, and consultant. Over the years, I have noticed a recurring pattern with business professionals: they often say, <span class="text-white font-medium">'I'm just not techy.'</span> I want to eliminate that mindset.
                </p>
                <p>
                  I started in web design about five years ago. Initially, I focused purely on aesthetics—creating beautiful, eye-catching sites. But I quickly realized that good looks alone don't generate results.
                </p>
                <p>
                  Today, I offer comprehensive SEO services at a single flat rate because I believe in going all-in with my clients. <span class="text-purple-300 font-semibold">One price, full service. That's how we win.</span>
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-32 px-6">
      <div class="max-w-[900px] mx-auto text-center animate-on-scroll" style="animation: animationIn 0.8s ease-out 0.8s forwards; animation-play-state: paused;">
        <h2 class="text-4xl md:text-6xl font-bold mb-6 bg-gradient-to-r from-white via-purple-200 to-white bg-clip-text text-transparent">
          Ready to Work Together?
        </h2>
        <p class="text-neutral-300 text-xl mb-12 leading-relaxed">
          Creating a website that works for you, and grows the business you've worked hard for.
        </p>
        <a 
          href="/contact" 
          class="shiny-cta inline-block magnetic-btn"
        >
          Get Started
        </a>
      </div>
    </section>

    <Footer />
  </main>
</Layout>

<script>
  // Lazy load video when page is ready
  if ('IntersectionObserver' in window) {
    const videoObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const video = entry.target as HTMLVideoElement;
          video.preload = 'auto';
          video.load();
          videoObserver.unobserve(video);
        }
      });
    }, { rootMargin: '50px' });
    
    const aboutVideo = document.getElementById('about-hero-video');
    if (aboutVideo) {
      videoObserver.observe(aboutVideo);
      // Fallback: start loading after 500ms if still not visible
      setTimeout(() => {
        const vid = aboutVideo as HTMLVideoElement;
        if (vid.preload === 'none') {
          vid.preload = 'auto';
          vid.load();
        }
      }, 500);
    }
  }
</script>
